{
  "private": true,
  "scripts": {
    "compiler:start": "node compiler_server/server.mjs",
    "dev:web": "node scripts/start_web_with_compiler.mjs",
    "compiler:docker": "docker compose -f docker-compose.compiler.yml up -d --build",
    "test:firestore-rules": "firebase emulators:exec --only firestore --project psueducode-apk \"node --test test/firestore.rules.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.0.0"
  },
  "dependencies": {
    "firebase-admin": "^14.2.0"
  }
}
